# Overview
OpenClaw is a massive, highly extensible project containing complex memory management. It utilizes a robust plugin system supporting vector databases (LanceDB), relational databases (SQLite), and search indexers (QMD).
